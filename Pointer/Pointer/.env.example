# Environment Configuration for POINTER-Pro
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to git!

# Server Configuration
# Specify your external IP address here
# The app will automatically derive the following URLs:
#   - LiveKit URL: ws://{EXTERNAL_IP}:7880
#   - VM API URL: http://{EXTERNAL_IP}:5000
# The Info.plist will also be automatically generated with NSAppTransportSecurity exceptions for this IP
EXTERNAL_IP=your.external.ip.address

# LiveKit Configuration
# LiveKit access token (generate from your LiveKit server)
# To generate a token, run the following command on your LiveKit server:
# ./livekit-cli create-token \
#   --api-key devkey \
#   --api-secret secret \
#   --join --room <LIVEKIT_ROOM> \
#   --identity ios_client \
#   --valid-for <DURATION>
LIVEKIT_TOKEN=your_access_token_here

# Room name (optional, defaults to "live")
LIVEKIT_ROOM=live
